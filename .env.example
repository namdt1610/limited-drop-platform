# Database Configuration (use DATABASE_URL for simplicity)
DATABASE_URL=./database.db

# Or individual variables:
# DB_PATH=./database.db

# NocoDB Configuration
NOCODB_PUBLIC_URL=http://localhost:8080
NOCODB_JWT_SECRET=your_jwt_secret_here
NOCODB_ADMIN_EMAIL=admin@donaldwatch.vn
NOCODB_ADMIN_PASSWORD=admin123

# PayOS Configuration
PAYOS_CLIENT_ID=your_payos_client_id
PAYOS_API_KEY=your_payos_api_key
PAYOS_CHECKSUM_KEY=your_payos_checksum_key

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Resend Email Configuration
RESEND_API_KEY=your_resend_api_key

# Frontend Configuration (for development)
VITE_API_BASE_URL=http://localhost:8080/api

# PayOS Return URL Configuration (where customer is redirected after payment)
# Used in backend for PayOS checkout requests
# Development: http://localhost:3000 (Alpine dev server)
# Production: https://yourdomain.com
FRONTEND_URL=http://localhost:3000

# CORS Configuration (allowed origins for frontend requests)
# Development: defaults to localhost:5173, localhost:3000, 127.0.0.1:5173
# Production: set to your frontend domain (e.g., https://yourdomain.com)
# Multiple origins not supported via env var yet; update main.go if needed
CORS_ORIGINS=http://localhost:3000
